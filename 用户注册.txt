一：用户填写手机号请求后端获取验证码
       生成验证码
       存放到redis中，key为手机号，value为验证码
       发送一条MQ消息传递手机号和验证码，阿里云短信向该手机发送一条验证码短信

二：用户填写验证码请求后端注册
       获取redis中的验证码和用户填写的做校验
      根据手机号做校验，判断之前是否有人用该手机号注册过
      将用户注册的数据保存到数据库 